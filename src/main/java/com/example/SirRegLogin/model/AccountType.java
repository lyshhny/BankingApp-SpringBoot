package com.example.SirRegLogin.model;

/**
 * AccountType Enum
 * Defines a set of name constants, in this case account types
 * Represents the types of account that an user can create within the dasboard
 */

public enum AccountType {
    SAVINGS,
    CHECKING,
    BUSINESS
}